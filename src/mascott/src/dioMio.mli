val solve : (Rewriting.Variable.t list) * (Rewriting.Variable.t list) -> (Rewriting.Variable.t * int) list list

val minimize : (Rewriting.Variable.t * int) list list -> (Rewriting.Variable.t * int) list list
